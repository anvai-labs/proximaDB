// Copyright (C) 2025 ProximaDB
// SPDX-License-Identifier: Apache-2.0
= ADR-033: Primary Storage Format Strategy — Own the Vertical Where It Pays, Keep Parquet at the Interop Seam

[cols="1,3"]
|===
|Status|**Accepted (as amended by ADR-094, 2026-09-21).** The targeting decision stands. Its load-bearing "< 2× on sequential scans" premise is **flagged unverified-pending-measurement** — see "Post-survey evidence" below.
|Date|2026-06-27
|Amended by|ADR-094 (unified storage substrate and compute seam)
|Deciders|Vijaykumar Singh
|Relates to|`CODESIGN_DIMENSIONAL_ARCHITECTURE_2026_06_19.adoc` (dominant cost = I/O round-trips + egress), `DATA_WAREHOUSE_AND_ENGINEERING_COURSE_CORRECTION_2026_06_04.adoc` (the dual-path pivot), ADR-026 (PAX canonical vector path), ADR-027 (unified storage + metering)
|Supersedes|—
|Tracked under|TD-162a-2 (typed `user_columns` PAX stripe → native OLAP scan) — this ADR **recasts its rationale and scope**; the routing cost loop (the Native arm is the second cost arm)
|===

== Context

A recurring design challenge: ProximaDB's **dual-path** architecture serves
relational/warehouse workloads with **DataFusion over standard Parquet**
(Iceberg-managed) and vector/ANN workloads with native engines over the custom
**PAX** block format. This raised a sharp co-design question: *why use Parquet at
all — shouldn't a co-designed database own a single primary format optimized for
the object-storage cost model, and relegate Parquet to a secondary export/interop
role?*

The TPC-H pgwire conformance suite is **22/22 green** over the Parquet/DataFusion
arm, so relational lowering is correct and the warehouse arm works. That makes the
*format* question — not the lowering question — the open frontier.

This ADR records the decision after a deep, adversarially-verified survey of how
the leading systems design their **primary** storage format (PostgreSQL, MySQL/
InnoDB, Oracle/Exadata, Amazon Aurora, Neon) and the columnar/lakehouse and
vector-DB landscape (ClickHouse, Snowflake, BigQuery, DuckDB, Delta/Iceberg,
Lance, Turbopuffer). 22 sources, 106 claims extracted, 25 adversarially verified
(24 confirmed, 1 refuted).

== Cost model (first principles — the spine)

The survey reports what others *converged on*; this section derives *why* from the
physics, so the format decisions rest on the cost model, not on imitation. For a
query whose storage substrate is object storage (per-GET RTT ~50-250 ms, billed
per-request + per-egress-GB, immutable objects), the cost decomposes into terms of
very different magnitude:

[.text-center]
....
  Latency(q) ≈ DEPTH(dependent-GET chain) × RTT  +  bytes_on_critical_path / bandwidth
  $(q)       ≈ requests × GET_fee  +  egress_bytes × egress_rate  +  compute_ms × c
....

The decisive fact: **100 GETs issued in parallel ≈ one GET's latency; 3 GETs issued
serially ≈ 3×.** Latency is the *depth* of the dependent-request chain, not the byte
total or the request count. This yields a strict priority order that the rest of the
ADR (and ADR-034) optimize against:

[.text-center]
**minimize dependency DEPTH ≫ minimize parallel-batch WIDTH ≫ minimize BYTES ≫ minimize CPU.**

Depth is latency (what kills you); width is throughput-bound (cheap); bytes are
egress-$; CPU is nearly free. *This is the real reason a co-designed format beats
Parquet:* not "fewer bytes," but **collapsing the footer→metadata→index→data serial
chain** that Parquet (and, internally today, our own ranged reader) pays.

=== Egress correction — two terms for two workloads

An earlier framing wrongly called quantization an "egress lever." Corrected:

* **Egress ($) is the customer-facing *result* only.** For an ANN top-k that is tiny (k × {id, score, small payload}); quantization does not change it (you rerank to f32 and return the same top-k).
* **Object-store → compute reads are internal / same-region** — metered as KOU *read* direction but priced ~0; they are **not** egress. Their cost is GET round-trips + transfer *time*, not egress dollars.
* Therefore **quantization is a round-trip / cache-residency lever, not an egress lever**: small codes (SQ8 ¼, RaBitQ ~1/32) keep the candidate corpus resident, turning cold GETs into warm reads — it attacks *depth/round-trips* (the dominant term for intra-region search), not egress.
* **Egress genuinely dominates** a *different* workload class — bulk export, wide OLAP result sets shipped to the client, embedding flows (KEU), and cross-region/cross-cloud reads (where the S3→compute hop *does* cross a billing boundary). Projection + result-side compression are the levers there.

So the co-design mandate's "dominant cost = round-trips **+** egress" is **two terms
for two workloads**: round-trips dominate intra-region search (quantization's
target); egress dominates export / wide-result / cross-region (projection's target).
Conflating them is the error this section corrects.

== Evidence (verified)

=== Nobody uses Parquet as a primary format

Every surveyed engine owns a primary format co-designed for its access pattern;
open columnar formats appear only at the interop edge:

* **PostgreSQL** — heap, 8KB pages, inline MVCC + TOAST (heap bloat / VACUUM is the cost).
* **MySQL/InnoDB** — clustered (index-organized) B+tree; latest version inline, old versions in undo.
* **Oracle/Exadata** — data blocks + **Hybrid Columnar Compression**, itself a **PAX-style** hybrid (columns grouped within a self-contained Compression Unit); **Smart Scan** offloads *projection + predicate filtering + decompression* to the storage cells (a storage⇄compute co-design; a 1 TB scan returning 2 MB ships only 2 MB to the DB tier).
* **Amazon Aurora** — *"the log is the database"*: only redo log records cross the network; pages are materialized in the storage tier on demand (35× more txns than mirrored MySQL at 7.7× fewer I/Os).
* **Neon** — log-structured **image+delta layer files** keyed by (key-range, LSN) over S3; pages reconstructed from WAL via `get_page_at_lsn`; copy-on-write branching falls out of the immutable layer model.
* **Snowflake** — proprietary **micro-partitions** are primary; Iceberg/Parquet is the *interop* option, with clustering/governance layered on top.
* **ClickHouse** (MergeTree + sparse primary index + min-max/set/Bloom skip indexes), **BigQuery** (Capacitor), **Lance**, **Turbopuffer** — all custom-primary.

=== Parquet's specific failures on object storage

* **Round-trip amplification** — one query can issue **dozens-to-hundreds of sequential dependent S3 GETs** (footer → row-group metadata → page index → data pages). At ~250 ms p90 per <1 MB GET, *round-trip count*, not bytes, dominates.
* **Coarse pruning** — a single per-row-group footer stats layer (ORC has 3; MergeTree adds a sparse primary index Parquet's footer can't replicate).
* **Poor random access** — point lookup is O(n) for variable-width data; reading one row touches a full ~1 MB page; metadata bloats to tens of MB; the page-offset index costs **~20 GiB/billion rows** (Lance caps an equivalent search cache at ~1.28 GiB).
* **No native secondary/vector indexes; weak update/delete** (immutable files defer mutation to a table-format layer).

=== Where the "replace Parquet" case is *overstated* (the refutations matter)

The adversarial pass killed or qualified several common arguments — recorded here
to keep the decision honest:

* ✗ *"Parquet must re-read the footer every query"* — **false**; solvable with metadata caching, not a format limit.
* ⚠️ For pure **sequential OLAP scans**, the custom-vs-Parquet gap is **< 2×**. The 100×-2000× Lance figures are for **random access / point lookup / vector search**, *not* scans.
* ⚠️ **Disaggregation (log-is-the-database) is not free**: workload-dependent (~2.5× write gain only under heavy load; *none* under light load), and read-after-write pays log-replay overhead (20-66% lag).
* ⚠️ **Parquet-as-primary IS right for lakehouse/Iceberg interop** — engine-agnostic cross-engine sharing over one physical copy (Snowflake productizes exactly this).
* The PAX paper itself (Ailamaki et al., VLDB'01) is a **cache/CPU** optimization (≥75% fewer L2 misses, 11-48% on TPC-H), *orthogonal* to I/O — PAX-on-object-storage value comes from the *layout discipline*, not the original cache result.

**Net:** the custom-format payoff is **concentrated in random-access / vector /
selective / round-trip-bound workloads (100×+), and is small (< 2×) for
sequential OLAP scans.**

[IMPORTANT]
====
**Amendment — this "Net" is the load-bearing premise of the whole Decision below, and it is
now contested. Treat it as unverified until measured (ADR-094, 2026-09-21).**

The survey did not include **Vortex**, which reports **100× faster random access *and*
10–20× faster scans and 5× faster writes at similar compression** — i.e. it claims the
random-access win *without* the scan trade-off this Net asserts is unavoidable. Vortex is
now a Linux Foundation project with a DuckDB extension (2026-01), so it is not a paper
design. **Lance 2.1** independently reports near-Parquet scan speed alongside fast random
access, achieved by separating *structural* from *compressive* encoding and choosing
structure by value width.

If either result holds for ProximaDB's shapes, "you must trade scans for random access"
is not a law, and this ADR's *targeting* logic (spend the custom-format budget only on the
vector path) weakens accordingly.

This is deliberately **not** resolved by assertion in either direction. The scale harness
landed in #1914 can measure it. Until a dated entry exists in
`docs/_internal/roadmap/BENCHMARK_EVIDENCE.toml`, no decision may cite "< 2× on scans" as
established fact — per mandate #6.
====

== Decision

. **The dual-path is vindicated, with a sharper rationale.** Keep **Parquet as the
  primary for the relational-OLAP-scan arm** — it is within ~2× of a custom format
  on scans *and* it is the Iceberg interop seam. This is the correct call there, not
  a compromise. Parquet is **not** demoted to "export only"; it is the legitimate
  primary for relational scan + cross-engine interop + reading externally-produced data.

. **Spend the custom-format (PAX) budget exactly where the data shows 100×:** the
  **vector/ANN + point-lookup + selective** path — which is already PAX (ADR-026).
  The co-design levers the survey most strongly supports are **round-trip
  minimization** and **format⇄index co-design**, not raw scan throughput.

. **Adopt an explicit object-storage round-trip budget on the PAX/vector path**
  (cf. Turbopuffer's ≤3 round-trips per search; ~444 ms cold → ~10 ms warm). The
  `io_trace` `range_gets` term + the route cost model already *measure* this — make
  it a first-class design constraint, and treat Parquet's footer round-trip
  amplification as the measured anti-pattern.

. **Co-optimize the PAX format WITH its indexes** (clustered layout, zone maps,
  secondary + vector indexes over the same dataset — the Lance pattern), rather
  than treating storage and indexing as separate layers.

== Consequences — recast TD-162a-2

TD-162a-2 (the typed `user_columns` PAX stripe + per-column zone maps that unblocks
the native OLAP scan over PAX) is **re-justified**: its value is **not** "beat
Parquet on OLAP scans" (that buys < 2×). Its real worth is:

* **(a) One co-designed store** for relational + vector — no dual materialization, and the round-trip budget applies uniformly.
* **(b) Enables the Native cost-router arm** — gives the routing cost loop a real second arm over PAX, closing the counterfactual-trace gap (the calibrated-surface caveat in the offline routing proof).
* **(c) Zone-map / projection pruning + round-trip control** on the **selective** queries where the custom format wins big — *not* the full-scan aggregates where Parquet already suffices.

Concretely: prioritize the typed stripe + per-column zone maps (pruning, round-trip
control) and the format⇄index co-design over chasing scan-throughput parity with
DataFusion/Parquet (already near-parity). Keep the Parquet/DataFusion warehouse arm
as-is; it is correct and ratcheted (TPC-H 22/22).

== Verification

* The thesis "no serious DB uses Parquet as primary" is confirmed across 5 incumbents + 4 columnar/vector systems.
* The "own a custom format" win is **quantified and bounded**: 100×+ on random access / vector; < 2× on sequential scans — so the investment is targeted, not wholesale.
* Out of scope (not decided here): the specific PAX stripe encoding (TD-162a-2 LLD), and whether the relational Native arm should ever supersede DataFusion for scans (it should not, on this evidence).

== Post-survey evidence (added by ADR-094, 2026-09-21)

Developments that postdate or were missed by the 2026-06-27 survey, recorded so the
Decision above is read against current facts:

* **Vortex** — extensible columnar format; reports 100× random access, 10–20× scans, 5×
  writes at similar compression, using lightweight encodings, late decompression and
  compute-on-compressed. Now an LF AI & Data incubation project with a DuckDB extension
  (2026-01). **Absent from this ADR's 22 sources.** Directly contests the "< 2× on scans"
  Net above.
* **Lance 2.1 adaptive structural encodings** (arXiv 2504.15247) — the *mechanism* for
  "one format, both access patterns": structural encoding (nesting/repetition/validity)
  separated from compressive encoding, with structure chosen by value width — full-zip for
  ≥128 B values (1 IOP fixed-width, 2 variable), mini-block below. Cuts the search cache
  from Parquet's ~20 GiB to ~1.28 GiB per billion-row vector column, reportedly without
  sacrificing scan speed or requiring per-column tuning.
* **DuckLake** — puts table metadata in a SQL database rather than files-in-bucket,
  collapsing Iceberg's `metadata.json → manifest list → manifest → data` dependent chain
  into one query. This is the *depth* lever this ADR's own cost spine ranks highest, applied
  at the table layer; ADR-094 Decision 4 applies it at the segment layer, where ProximaDB
  already owns a catalog.
* **Iceberg v3 is GA** (ratified mid-2025; shipped through 1.11 by May 2026) — deletion
  vectors (Roaring bitmaps in Puffin sidecars), row lineage, geospatial, and **Variant with
  shredding**. Delta and Iceberg are converging on identical deletion-vector encodings and
  compatible row tracking. Notably, ProximaDB's P-Shred (ADR-055) arrived at variant
  shredding independently — convergent evolution that both validates the design and argues
  for aligning its semantics with the spec for interop.
* **Databricks Mosaic AI Vector Search**, for contrast — the vector index is a *separate
  artifact synced from* the Delta table, which buys sync lag, double storage, and no
  transactional table↔index consistency. ProximaDB's one-substrate model is the
  differentiator this ADR's dual-path framing does not name.

== Sources

Aurora SIGMOD'17 + OpenAurora SIGMOD'24; Neon pageserver docs + get-page-at-lsn;
PAX (Ailamaki et al., VLDB'01); Oracle HCC whitepaper + Exadata Smart-Scan docs;
Lance format; Turbopuffer architecture; ClickHouse / Databend columnar-format
engineering notes; DataFusion external-Parquet-indexes; Iceberg/Delta interop
(Dremio, Snowflake). Adversarially verified (24 confirmed / 1 refuted of 25).
